[project]
name = "xora-poc-backend"
version = "0.1.0"
description = "Backend Server (Temporal Client mỏng) cho Xora POC — Dynamic Agent Loop"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn[standard]==0.32.1",
  "temporalio==1.9.0",
  "pydantic==2.10.3",
  "sqlalchemy==2.0.36",
  "asyncpg==0.30.0",
  "alembic==1.14.0",
]

[tool.uv]
package = false
