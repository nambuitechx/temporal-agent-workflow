# Đích proxy /api lúc `npm run dev` / `npm run preview` (đọc ở vite.config.ts,
# KHÔNG cần tiền tố VITE_). Container nginx dùng biến API_UPSTREAM riêng
# (xem Dockerfile + docker-compose.yml), không liên quan file này.
# API_URL=http://localhost:8000
